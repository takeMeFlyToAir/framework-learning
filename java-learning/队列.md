### PriorityQueue
```
优先级队列：
1、新增元素的时候，只需要保证最小的元素在头部
2、删除元素的时候，再次调整元素顺序，将最小的元素排到第一位

所以说队列内部整体是无序的，只是每次新增或者删除的时候会实时调整顺序，那么它本身的性能肯定不好
总结
1>PriorityQueue是一种无界的，线程不安全的队列
2>PriorityQueue是一种通过数组实现的，并拥有优先级的队列
3>PriorityQueue存储的元素要求必须是可比较的对象， 如果不是就必须明确指定比较器

```